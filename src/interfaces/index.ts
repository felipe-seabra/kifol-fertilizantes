export interface IFormValues {
  name: string;
  email: string;
  phone: string;
  message: string;
  checkbox: boolean;
}

export interface IVideosHome {
  id: number;
  title: string;
  url: string;
  description: string;
}

export interface IVideosPage {
  id: number;
  url: string;
}
